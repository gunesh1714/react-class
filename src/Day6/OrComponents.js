import { useInsertionEffect } from "react";

export default function EnComponents(OrComponents)
{
    class NewComponents extends Component
    {
        state={count:0}
        inc=()=>
        {this.setState({count:this.state.count+1})

        render ()
        {
        
                
            
        }
    }
    }
}